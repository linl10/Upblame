数组 
1.a为二维数组 p指针 p指向第一维数组的第一个数据 以次类推 第二组数组的第二个数据

常量
1.常量不能被赋值

%使用
1.%f 既可用于double 也可 float

字符
1.printf("%s%s","12345678"+4,b+8);     //字符常量名和字符串变量名都代表指向字符串的首地址  
 “12345678”+4 指5的指针  +为后移 -为前移

编程
1.如在找不到的时候需要输出一个值 则先定义这个值 在接下来的操作中如找到 即可替换 //编程思路

指针
1.在主函数传入的&a参数 在定义函数里定义的是指针 即把地址的值放在指针里   //sun(&a,&b);  void sun(int *a,int *b)
2.指针+1指的是指针后移 而不是指针位+1
3.指向下一个地址时的表达方式为&p+i   而不是&（p+i）

